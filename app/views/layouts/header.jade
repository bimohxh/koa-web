meta(charset="utf-8")
meta(name="viewport" content="width=device-width, initial-scale=1")
title 搭配家后台管理系统 

if localEnv.environment == 'development'
  include ./devHeader



//- 存入后端的某些公共变量供JS使用  全局配置
script.
  window.SITE = {
    router: {
      controller: '#{route.controller}',
      action: '#{route.action}'
    }

  }
  //console.log("ww", "#{route.controller}.#{route.action}")


if localEnv.environment == 'development'
  include ./devFooter
